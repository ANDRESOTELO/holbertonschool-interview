#ifndef SORT_H
#define SORT_H

#include <stddef.h>
#include <stdlib.h>

/* Function to print */

void print_array(const int *array, size_t size);

/* Function to sort */

void merge_sort(int *array, size_t size);

#endif /* SORT_H */
